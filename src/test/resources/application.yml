spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/spring_library_test
    username: postgres
    password: pass
    initialization-mode: always

  shell:
    interactive:
      enabled: false
